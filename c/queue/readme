1、队列的基本操作是Enqueue（入队），它是在末端（叫做队尾 rear）插入一个元素，还有Dequeue（出队），他是删除（或返回）在表的开头（叫做队头 front）的元素。
2、和栈一样，任何表的实现都是合法的。和栈一样，对于每一种操作，链表和数组都给出快速的O(1)运行时间。
3、Front或Rear到达数组的尾端，就又绕回来，叫做循环数组实现。要注意两件事，检测队列是否为空很重要；对于使用不同的方法表示队头和队尾时。

todo:使用链表实现队列
