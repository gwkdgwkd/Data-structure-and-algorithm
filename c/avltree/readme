1、AVL树是带有平衡条件的二叉查找树。平衡条件要容易保持，而且必须保证树的深度是O(logN)。最简单的想法是要求左右子树具有相同的高度，并不强求树的深浅要浅。
2、一颗AVL树是其每个节点的左子树和右子树的高度最多差1的二叉查找树（空树高度为-1）。
3、对于插入外边（左-左，右-右），通过对树的一次单旋转而完成调整。第二种情况发生在插入内部（左-右，右-左），通过稍微复杂的双旋转完成调整。
